	<%= form.error_notification %>
	<%= form.input :name, autofocus: true  %> (i.e., <%= "#{Faker::Company.name}" %>)
	<%= form.input :description %>
	<%= form.input :address %>
	<%= form.input :phone %>